# 服务端口（同时托管静态站 / API / admin）
PORT=3000

# JWT 配置（生产环境请改成随机长字符串，至少 32 字符）
JWT_SECRET=change_me_to_a_long_random_string_at_least_32_chars
JWT_EXPIRES_IN=12h

# 初始 admin 账户（首次启动时 seed 到 users 表，之后改密码请用 DB 或后续接口）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me_now
